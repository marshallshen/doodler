require 'spec_helper'

describe Doodler::Polygon do
  describe "#add_point" do
    context "given 1 exisiting point" do
      it "should draw another point that is within a certain radius of the first point"
    end
  end
end
